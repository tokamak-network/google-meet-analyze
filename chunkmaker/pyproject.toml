[project]
name = "chunkmaker"
version = "0.1.0"
description = "ChunkMaker v1 for ATI meeting transcripts"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
